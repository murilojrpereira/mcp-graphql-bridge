# Ignore the compiled output.
dist/

# TypeScript incremental compilation cache
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage (from Jest)
coverage/
__temp

# JUnit Reports (used mainly in CircleCI)
reports/
junit.xml

# Node modules
node_modules/

# .env to store APOLLO_KEY
.env

# Mac OS
.DS_Store

# Claude Code — settings.json contains secrets (tokens), never commit
.claude/settings.json
.claude/settings.local.json

# Intellij Configuration Files
.idea/

generated-schema.graphql

# Local GraphQL schema snapshot (API-specific, not for sharing)
schema-introspection.json